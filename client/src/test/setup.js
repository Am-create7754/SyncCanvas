// Phase 13A: no framework-specific test matchers needed anymore (every test here runs
// against plain JS modules / DOM APIs directly) — kept as an empty hook point in case a
// future test suite needs global setup again.
